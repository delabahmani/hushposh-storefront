import { Category } from "@prisma/client";

type CategoryProps = {
  categories: Category[];
};

export default function Categories({categories}: CategoryProps) {
 
 
 
 
  return <div>
    
  </div>;
}
